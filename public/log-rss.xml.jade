!!!xml
rss(version='2.0')
  channel
    title #{ title }
    link #{ site_url }/
    each log, slug in public.log._data
      item
        title #{ log.title }
        link #{ log.url }
        description <![CDATA[!{ log.description }]]>
